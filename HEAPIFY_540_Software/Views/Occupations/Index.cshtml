@model IEnumerable<HEAPIFY_540_Software.Models.Occupation>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
           Position Title
        </th>
        <th>
            Employer Name
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PositionTitle)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EmployerName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.OccupationID }) |
            @Html.ActionLink("Details", "Details", new { id=item.OccupationID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.OccupationID })
        </td>
    </tr>
}

</table>

<div>

    <h6>If you were adding a patient's demographic, please click this @Html.ActionLink("link", "Create", "Demographics")</h6>

   



</div>
