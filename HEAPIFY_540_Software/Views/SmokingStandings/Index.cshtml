@model IEnumerable<HEAPIFY_540_Software.Models.SmokingStanding>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.SmokingStatusType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.SmokingStatusType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SmokingStatusID }) |
            @Html.ActionLink("Details", "Details", new { id=item.SmokingStatusID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SmokingStatusID })
        </td>
    </tr>
}

</table>
