@model IEnumerable<HEAPIFY_540_Software.Models.PhoneNumber>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Phone Number
        </th>
        <th>
           Phone Type
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PhoneNumber1)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PhoneType.PhoneTypeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PhoneNumberID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PhoneNumberID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PhoneNumberID })
        </td>
    </tr>
}

</table>

<div>

    <h6>If you were adding a patient, please click this @Html.ActionLink("link", "Create", "Patients") to go back</h6>

    <h6>If you were adding an employee, please click the @Html.ActionLink("link", "Create", "Employees") to go back</h6>

    <h6>If you were adding an emergency contact, please click the @Html.ActionLink("link", "Create", "EmergencyContacts") to go back</h6>

</div>
