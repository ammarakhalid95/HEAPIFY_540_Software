@model IEnumerable<HEAPIFY_540_Software.Models.PatientEmergencyContact>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.EmergencyContact.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Patient.FirstName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.EmergencyContact.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Patient.FirstName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PatientEmergencyContactID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PatientEmergencyContactID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PatientEmergencyContactID })
        </td>
    </tr>
}

</table>
