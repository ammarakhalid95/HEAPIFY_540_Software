@model IEnumerable<HEAPIFY_540_Software.Models.AlchoholStanding>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AlcoholStatusType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AlcoholStatusType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AlcoholStatusID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AlcoholStatusID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AlcoholStatusID })
        </td>
    </tr>
}

</table>
