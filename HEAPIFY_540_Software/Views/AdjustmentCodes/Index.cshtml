@model IEnumerable<HEAPIFY_540_Software.Models.AdjustmentCode>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AdjustmentDescription)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AdjustmentDescription)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AdjustmentCodeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.AdjustmentCodeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AdjustmentCodeID })
        </td>
    </tr>
}

</table>
