@model IEnumerable<HEAPIFY_540_Software.Models.ActiveSStanding>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ActiveSSType)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ActiveSSType)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ActiveSStatusID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ActiveSStatusID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ActiveSStatusID })
        </td>
    </tr>
}

</table>
